# 塊設備用戶指南 {#bdev}

## 目標讀者 {#bdev_ug_targetaudience}

本用戶指南面向了解塊存儲、存儲驅動程式、發出 JSON-RPC
命令以及存儲服務（如 RAID、壓縮、加密等）的軟體開發人員。

## 簡介 {#bdev_ug_introduction}

SPDK 塊設備層，通常簡稱為 *bdev*，是一個 C 函式庫，
旨在等同於操作系統塊存儲層，該層
通常位於傳統內核
存儲堆棧中設備驅動程式之上。具體來說，此函式庫提供以下
功能：

* 用於實現與不同類型塊存儲設備介面的塊設備的可插拔模組 API。
* 適用於 NVMe、malloc（ramdisk）、Linux AIO、virtio-scsi、Ceph RBD、Pmem 和 Vhost-SCSI 啟動器等的驅動程式模組。
* 用於枚舉和聲明 SPDK 塊設備然後對這些設備執行操作（讀取、寫入、取消映射等）的應用程式 API。
* 用於堆疊塊設備以創建複雜 I/O 管道的設施，包括邏輯卷管理 (lvol) 和分區支持 (GPT)。
* 通過 JSON-RPC 配置塊設備。
* 請求隊列、超時和重置處理。
* 用於向塊設備發送 I/O 的多個無鎖隊列。

Bdev 模組創建抽象層，為所有設備提供通用 API。
用戶可以使用可用的 bdev 模組或創建自己的模組，其下具有任何類型的
設備（有關詳細資訊，請參閱 @ref bdev_module）。SPDK
還提供 vbdev 模組，這些模組在現有 bdev 上創建塊設備。例如
@ref bdev_ug_logical_volumes 或 @ref bdev_ug_gpt

### SPDK Bdev 架構圖 {#bdev_ug_architecture}

以下圖表展示了 SPDK bdev 層的整體架構：

```
┌─────────────────────────────────────────────────────────────┐
│                    Application Layer                         │
│              (使用 bdev API 進行 I/O 操作)                    │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                    Bdev Layer (抽象層)                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Bdev API   │  │  I/O Queue   │  │   vbdev      │      │
│  │  (通用介面)  │  │  (無鎖隊列)  │  │  (虛擬設備)  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└──────────────────────┬──────────────────────────────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
┌───────▼──────┐ ┌────▼──────┐ ┌────▼──────┐
│  物理 Bdev   │ │  虛擬 Bdev │ │  堆疊 Bdev│
│              │ │            │ │            │
│ • NVMe       │ │ • Crypto   │ │ • Lvol     │
│ • Malloc     │ │ • Delay    │ │ • GPT      │
│ • AIO        │ │ • OCF      │ │ • Split    │
│ • RBD        │ │            │ │            │
│ • iSCSI      │ │            │ │            │
└──────────────┘ └────────────┘ └────────────┘
        │              │              │
        └──────────────┼──────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│                 底層存儲設備                                  │
│  (NVMe SSD, 網路存儲, 本地文件系統等)                         │
└─────────────────────────────────────────────────────────────┘
```

### 名詞解釋 {#bdev_ug_glossary}

#### 核心概念

- **Bdev (Block Device)**: 塊設備，SPDK 中對存儲設備的抽象表示。所有存儲設備（無論是物理還是虛擬）在 SPDK 中都表現為 bdev。

- **物理 Bdev**: 直接對應物理存儲設備的 bdev，例如 NVMe SSD、通過 AIO 訪問的本地磁盤等。

- **虛擬 Bdev (vbdev)**: 在現有 bdev 之上創建的邏輯塊設備，提供額外功能（如加密、快取、延遲模擬等）。

- **堆疊 Bdev**: 可以將多個 bdev 組合或分層的 bdev，例如邏輯卷管理 (lvol) 或分區表 (GPT)。

#### 技術術語

- **JSON-RPC**: 一種遠程過程調用協議，使用 JSON 格式進行數據交換。SPDK 使用 JSON-RPC 進行配置和管理。

- **無鎖隊列 (Lockless Queue)**: 一種並發數據結構，允許多個線程同時訪問而不需要互斥鎖，提高性能。

- **LBA (Logical Block Address)**: 邏輯塊地址，用於標識塊設備上的數據位置。

- **GUID (Globally Unique Identifier)**: 全局唯一標識符，在 GPT 分區表中用於標識分區類型。

- **IQN (iSCSI Qualified Name)**: iSCSI 協議中用於唯一標識啟動器和目標的名稱格式。

- **NQN (NVMe Qualified Name)**: NVMe 協議中用於唯一標識 NVMe 子系統的名稱格式。

#### 存儲相關

- **RBD (RADOS Block Device)**: Ceph 分佈式存儲系統提供的塊設備介面。

- **RADOS**: Ceph 的可靠自主分佈式對象存儲系統，是 Ceph 的底層存儲引擎。

- **NVMe-oF (NVMe over Fabrics)**: 通過網路（如 RDMA、TCP）訪問遠程 NVMe 設備的協議。

- **Hugepage**: 大頁內存，比標準 4KB 頁面更大的內存頁面，可以減少 TLB 缺失，提高性能。

#### 加密相關

- **AES (Advanced Encryption Standard)**: 高級加密標準，一種對稱加密算法。

- **CBC (Cipher Block Chaining)**: 密碼塊鏈接模式，一種塊密碼操作模式。

- **XTS (XEX-based Tweaked CodeBook)**: 一種磁盤加密模式，適合用於塊設備加密。

- **IV (Initialization Vector)**: 初始化向量，用於加密算法中增加隨機性。

- **QAT (QuickAssist Technology)**: Intel 的硬體加速技術，用於加速加密和壓縮操作。

#### 快取相關

- **OCF (Open CAS Framework)**: 開源快取加速框架，提供高性能塊存儲快取功能。

- **Write-Through**: 寫透模式，數據同時寫入快取和後端存儲。

- **Write-Back**: 寫回模式，數據先寫入快取，稍後再寫入後端存儲。

#### 網路相關

- **NBD (Network Block Device)**: 網路塊設備，允許將遠程塊設備作為本地塊設備使用。

- **iSCSI (Internet Small Computer Systems Interface)**: 基於 IP 網路的 SCSI 協議，用於遠程訪問存儲設備。

- **RDMA (Remote Direct Memory Access)**: 遠程直接內存訪問，允許網路適配器直接訪問內存，無需 CPU 參與。

## 先決條件 {#bdev_ug_prerequisites}

本指南假設您已經可以在您的平台上構建標準 SPDK 分發版。
塊設備層是一個 C 函式庫，具有一個名為 bdev.h 的公共
頭文件。以下章節中描述的所有 SPDK 配置都是通過使用 JSON-RPC 命令完成的。
SPDK 提供了一個基於 python 的
命令行工具，用於發送位於 `scripts/rpc.py` 的 RPC 命令。用戶
可以通過使用 `-h` 或 `--help` 標誌運行此腳本來列出可用命令。
此外，用戶可以通過運行 `scripts/rpc.py rpc_get_methods` 直接從 SPDK 應用程式檢索當前支持的 RPC 命令集。
可以通過添加 `-h` 標誌作為
命令參數來顯示每個命令的詳細幫助。

## 配置塊設備模組 {#bdev_ug_general_rpcs}

可以使用 JSON RPC 配置塊設備。可用 RPC 命令的完整列表
以及詳細資訊可以在 @ref jsonrpc_components_bdev 頁面上找到。

## 常見塊設備配置範例

## Ceph RBD {#bdev_config_rbd}

### 概述

SPDK RBD bdev 驅動程式提供對 Ceph RADOS 塊
設備 (RBD) 的 SPDK 塊層訪問。Ceph RBD 設備通過 librbd 和 librados 函式庫訪問
由 Ceph 導出的 RADOS 塊設備。要創建 Ceph bdev RPC
命令，應使用 `bdev_rbd_register_cluster` 和 `bdev_rbd_create`。

### 架構說明

```
┌─────────────────────────────────────────────────────────────┐
│                    SPDK Application                         │
│                                                             │
│  ┌──────────────┐      ┌──────────────┐                    │
│  │  RBD Bdev 1  │      │  RBD Bdev 2  │                    │
│  │  (獨立集群)  │      │  (共享集群)  │                    │
│  └──────┬───────┘      └──────┬───────┘                    │
│         │                     │                             │
│         │  ┌──────────────────┘                             │
│         │  │                                                 │
│         ▼  ▼                                                 │
│  ┌─────────────────────────────────────┐                    │
│  │    Rados Cluster Object             │                    │
│  │  (io_context_pool + messenger)      │                    │
│  └──────────────┬──────────────────────┘                    │
└─────────────────┼───────────────────────────────────────────┘
                  │
                  │ librbd / librados
                  │
┌─────────────────▼───────────────────────────────────────────┐
│                    Ceph Cluster                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                 │
│  │  OSD 1   │  │  OSD 2   │  │  OSD 3   │                 │
│  └──────────┘  └──────────┘  └──────────┘                 │
│                                                             │
│  ┌─────────────────────────────────────┐                   │
│  │      RBD Image (foo)                │                   │
│  │      in Pool (rbd)                  │                   │
│  └─────────────────────────────────────┘                   │
└─────────────────────────────────────────────────────────────┘
```

### 兩種創建方式

SPDK 提供了兩種創建 RBD bdev 的方法：

1. **獨立集群模式**: 為每個 RBD bdev 創建一個新的 Rados 集群對象
   - 優點：隔離性好，每個 bdev 有獨立的連接
   - 缺點：資源消耗較大，每個集群對象都會創建線程

2. **共享集群模式**: 為多個 RBD bdev 共享同一個 Rados 集群對象
   - 優點：資源利用率高，減少線程數量
   - 缺點：需要合理規劃共享的 bdev 數量

每個 Rados 集群對象創建少量 io_context_pool 和 messenger 線程。
Ceph 命令 `ceph config help librados_thread_count` 和 `ceph config help ms_async_op_threads`
可以幫助檢查這些線程資訊。此外，您可以通過
更新 ceph.conf 文件或使用 Ceph 配置命令來指定線程數。有關更多資訊，請參閱
[Ceph 配置](https://docs.ceph.com/en/latest/rados/configuration/ceph-conf/)

### 性能考量

對於大量 RBD bdev，一組線程可能不足以最大化性能，
但每個 RBD bdev 一組線程可能會增加太多上下文切換。因此，可能需要
對每個集群對象的 RBD bdev 數量和線程進行性能調整。

**建議**：
- 少量 bdev（< 10）：可以使用共享集群模式
- 中等數量 bdev（10-50）：創建 2-3 個共享集群，每個集群管理多個 bdev
- 大量 bdev（> 50）：需要根據實際 I/O 負載進行測試和調整

範例命令

`rpc.py bdev_rbd_register_cluster rbd_cluster`

此命令將註冊一個名為 rbd_cluster 的集群。可選的 `--config-file` 和
`--key-file` 參數為集群指定。

要刪除已註冊的集群，請使用 bdev_rbd_unregister_cluster 命令。

`rpc.py bdev_rbd_unregister_cluster rbd_cluster`

要使用已註冊的集群創建 RBD bdev。

`rpc.py bdev_rbd_create rbd foo 512 -c rbd_cluster`

此命令將創建一個 bdev，表示來自名為 'rbd' 的池的 'foo' 映像。
當為 `bdev_rbd_create` 指定 -c 時，RBD bdev 將在 librbd 模組中與 Ceph 的一個連接共享同一個 rados 集群。
相反，如果不指定 -c，它將為每個 bdev 創建一個新的 rados 集群，每個集群有一個
集群連接。

要刪除塊設備表示，請使用 bdev_rbd_delete 命令。

`rpc.py bdev_rbd_delete Rbd0`

要調整 bdev 大小，請使用 bdev_rbd_resize 命令。

`rpc.py bdev_rbd_resize Rbd0 4096`

此命令將 Rbd0 bdev 調整為 4096 MiB。

## 加密虛擬 Bdev 模組 {#bdev_config_crypto}

### 概述

可以配置加密虛擬 bdev 模組以提供靜態數據加密
適用於任何底層 bdev。該模組依賴 SPDK Accel 框架來提供
所有加密功能。
其中一個 accel 模組，dpdk_cryptodev 使用 DPDK CryptoDev API 實現，
它支持許多不同的僅軟體加密模組以及硬體
輔助支持 Intel QAT 板和 NVIDIA 加密啟用的 NIC。

### 加密流程圖

```
┌─────────────────────────────────────────────────────────────┐
│                    Application                               │
│              (讀取/寫入請求)                                 │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│              Crypto Bdev (CryNvmeA)                          │
│                                                              │
│  寫入流程：                                                   │
│  ┌─────────────┐                                            │
│  │ 原始數據    │ ──┐                                         │
│  └─────────────┘   │                                         │
│                    │ 加密                                    │
│                    ▼                                         │
│  ┌─────────────┐  ┌─────────────┐                          │
│  │ 暫存緩衝區  │→ │ 加密數據    │                            │
│  └─────────────┘  └─────────────┘                          │
│                                                              │
│  讀取流程：                                                   │
│  ┌─────────────┐                                            │
│  │ 加密數據    │ ──┐                                         │
│  └─────────────┘   │                                         │
│                    │ 解密                                    │
│                    ▼                                         │
│  ┌─────────────┐                                            │
│  │ 原始數據    │ (直接寫入應用緩衝區)                        │
│  └─────────────┘                                            │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│              Accel Framework                                 │
│  ┌──────────────┐  ┌──────────────┐                        │
│  │ dpdk_cryptodev│  │ SW accel     │                        │
│  │ (硬體加速)   │  │ (ISA-L)      │                        │
│  └──────────────┘  └──────────────┘                        │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│              Base Bdev (NVMe1n1)                             │
│              (存儲加密後的數據)                               │
└─────────────────────────────────────────────────────────────┘
```

### I/O 處理說明

對於讀取，提供給加密塊設備的緩衝區將用作未加密數據的目標緩衝區。
但是，對於寫入，使用臨時暫存緩衝區作為
加密的目標緩衝區，然後將其傳遞給底層 bdev 作為
寫入緩衝區。這樣做是為了避免在原始源緩衝區中加密數據，這
可能會在某些用例中造成問題。

**設計原因**：
- 保護應用程式的原始數據緩衝區不被修改
- 允許應用程式在加密完成後繼續使用原始緩衝區
- 提供更好的錯誤處理和回滾機制

以下是支持加密操作的 accel 模組的資訊：

### dpdk_cryptodev accel 模組

支持以下密碼：
- AESN-NI 多緩衝區加密輪詢模式驅動程式：RTE_CRYPTO_CIPHER_AES128_CBC
- Intel(R) QuickAssist (QAT) 加密輪詢模式驅動程式：RTE_CRYPTO_CIPHER_AES128_CBC、
  RTE_CRYPTO_CIPHER_AES128_XTS
  （注意：QAT 功能正常，但在硬體完全集成到 SPDK CI 系統之前被標記為實驗性。）
- MLX5 加密輪詢模式驅動程式：RTE_CRYPTO_CIPHER_AES256_XTS、RTE_CRYPTO_CIPHER_AES512_XTS

### LBA 作為 IV 的處理機制

為了支持使用 bdev 塊偏移 (LBA) 作為初始化向量 (IV)，
加密模組將所有 I/O 分解為大小等於塊
大小的加密操作。例如，對塊大小為 512B 的 bdev 進行 4K I/O，
將導致 8 個加密操作。

**示例說明**：
```
應用程式請求：讀取 4KB 數據，起始 LBA = 1000
塊大小：512B

分解為：
- 操作 1: LBA 1000, 512B, IV = 1000
- 操作 2: LBA 1001, 512B, IV = 1001
- 操作 3: LBA 1002, 512B, IV = 1002
- ...
- 操作 8: LBA 1007, 512B, IV = 1007

每個操作使用對應的 LBA 作為 IV，確保相同數據在不同位置
加密後的結果不同，提高安全性。
```

### SW accel 模組

支持以下密碼：

- 使用 128 或 256 位密鑰實現的 AES_XTS 密碼，使用 ISA-L_crypto 實現

### 一般工作流程
- 設置所需的 accel 模組以執行加密操作，可以通過 `accel_assign_opc` RPC 命令完成
- 使用 `accel_crypto_key_create` RPC 命令創建命名加密密鑰。密鑰將使用分配的 accel
  模組。每個 accel 模組中的參數集和支持的密碼可能不同。
- 使用 `bdev_crypto_create` RPC 命令創建虛擬加密塊設備，提供基礎塊設備名稱和加密密鑰名稱

#### 範例

使用 dpdk_cryptodev accel 模組的範例命令
```
# 使用 `--wait-for-rpc` 參數啟動 SPDK 應用程式
rpc.py dpdk_cryptodev_scan_accel_module
rpc.py dpdk_cryptodev_set_driver crypto_aesni_mb
rpc.py accel_assign_opc -o encrypt -m dpdk_cryptodev
rpc.py accel_assign_opc -o decrypt -m dpdk_cryptodev
rpc.py framework_start_init
rpc.py accel_crypto_key_create -c AES_CBC -k 01234567891234560123456789123456 -n key_aesni_cbc_1
rpc.py bdev_crypto_create NVMe1n1 CryNvmeA -n key_aesni_cbc_1
```

這些命令將在 NVMe bdev
'NVMe1n1' 之上創建一個名為 'CryNvmeA' 的加密 vbdev，並將使用名為 `key_aesni_cbc_1` 的密鑰。
密鑰將與已分配用於加密操作的 accel 模組一起工作，在此範例中，它將是 dpdk_cryptodev。

### 加密密鑰格式

請確保密鑰以十六進制格式提供。這意味著傳遞給
rpc.py 的字串必須是二進制形式密鑰長度的兩倍。
                                                               
#### 範例命令

`rpc.py accel_crypto_key_create -c AES_XTS -e 7859243a027411e581e0c40a35c8228f -k 10fee72b3d47553e065affdb48c54a81 -n sample_key`

此命令將創建一個名為 `sample_key` 的密鑰，AES 密鑰
'10fee72b3d47553e065affdb48c54a81' 和 XTS 密鑰
'7859243a027411e581e0c40a35c8228f'。換句話說，要使用的複合 AES_XTS 密鑰是
'10fee72b3d47553e065affdb48c54a817859243a027411e581e0c40a35c8228f'

### 刪除虛擬加密塊設備

要刪除 vbdev，請使用 bdev_crypto_delete 命令。

`rpc.py bdev_crypto_delete CryNvmeA`

### dpdk_cryptodev mlx5_pci 驅動程式配置

mlx5_pci 驅動程式與啟用加密的 Nvidia NIC 一起工作，需要特殊配置
DPDK 環境以啟用加密功能。可以通過配置 SPDK 事件函式庫來完成
`spdk_app_opts` 結構的 `env_context` 成員或通過傳遞相應的 CLI 參數
以下形式：`--allow=BDF,class=crypto,wcs_file=/full/path/to/wrapped/credentials`，例如
`--allow=0000:01:00.0,class=crypto,wcs_file=/path/credentials.txt`。

## 延遲 Bdev 模組 {#bdev_config_delay}

### 概述

延遲 vbdev 模組旨在在較低
級別 bdev 之上應用預定的額外延遲。這使得在功能
或可擴展性測試期間能夠模擬設備的延遲特性。例如，為了模擬驅動延遲的影響，當
處理 I/O 時，可以在其上配置一個 NULL bdev 和一個延遲 bdev。

### 架構示意圖

```
┌─────────────────────────────────────────────────────────────┐
│                    Application                               │
│              (I/O 請求)                                      │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│              Delay Bdev (delay0)                             │
│                                                              │
│  ┌──────────────────────────────────────┐                   │
│  │  讀取請求                            │                   │
│  │  ┌────────────────────────────────┐  │                   │
│  │  │ 平均延遲: 10μs                 │  │                   │
│  │  │ P99 延遲: 50μs                 │  │                   │
│  │  └────────────────────────────────┘  │                   │
│  │                                      │                   │
│  │  寫入請求                            │                   │
│  │  ┌────────────────────────────────┐  │                   │
│  │  │ 平均延遲: 30μs                 │  │                   │
│  │  │ P99 延遲: 90μs                 │  │                   │
│  │  └────────────────────────────────┘  │                   │
│  └──────────────────────────────────────┘                   │
│                       │                                     │
│                       │ (應用延遲後)                        │
└───────────────────────┼─────────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────────┐
│              Base Bdev (Null0)                               │
│              (實際存儲設備)                                  │
└─────────────────────────────────────────────────────────────┘
```

### 使用場景

延遲 bdev 模組不旨在提供特定 NVMe 驅動延遲的高保真複製，
相反，它的主要目的是提供"大局"理解，了解通用延遲如何影響給定的
應用程式。

**典型應用場景**：
- **性能測試**: 模擬不同延遲條件下的應用程式表現
- **壓力測試**: 測試應用程式在高延遲環境下的穩定性
- **協議開發**: 測試上層協議對延遲的處理能力
- **調試**: 重現特定延遲相關的問題

延遲 bdev 是使用 `bdev_delay_create` RPC 創建的。此 rpc 接受 6 個參數，一個用於名稱
延遲 bdev 和一個用於基礎 bdev 的名稱。其餘四個參數表示以下
延遲值：平均讀取延遲、平均寫入延遲、p99 讀取延遲和 p99 寫入延遲。
### 延遲參數說明

在延遲 bdev 的上下文中，p99 延遲意味著百分之一的 I/O 將被延遲至少
在完成到上層協議之前，p99 延遲的值。所有延遲值
都以微秒為單位測量。

**參數解釋**：
- **平均讀取延遲 (Average Read Latency)**: 讀取操作的平均延遲時間
- **平均寫入延遲 (Average Write Latency)**: 寫入操作的平均延遲時間
- **P99 讀取延遲 (P99 Read Latency)**: 99% 的讀取操作延遲低於此值，1% 的讀取操作延遲等於或高於此值
- **P99 寫入延遲 (P99 Write Latency)**: 99% 的寫入操作延遲低於此值，1% 的寫入操作延遲等於或高於此值

**延遲分佈示例**：
```
假設配置：平均讀取延遲 = 10μs, P99 讀取延遲 = 50μs

實際延遲分佈：
- 大部分讀取操作：~10μs (平均值)
- 少數讀取操作：10μs - 50μs 之間
- 極少數讀取操作 (1%)：≥ 50μs (P99 值)
```

範例命令：

`rpc.py bdev_delay_create -b Null0 -d delay0 -r 10 --nine-nine-read-latency 50 -w 30 --nine-nine-write-latency 90`

此命令將創建一個延遲 bdev，平均讀取和寫入延遲分別為 10 和 30 微秒，p99 讀取
和寫入延遲分別為 50 和 90 微秒。

可以使用 `bdev_delay_delete` RPC 刪除延遲 bdev

範例命令：

`rpc.py bdev_delay_delete delay0`

## GPT (GUID 分區表) {#bdev_config_gpt}

### 概述

GPT 虛擬 bdev 驅動程式預設啟用，不需要任何配置。
它會自動檢測任何附加 bdev 上的 @ref bdev_ug_gpt 並將創建
可能的多個虛擬 bdev。

### GPT 分區結構示意圖

```
┌─────────────────────────────────────────────────────────────┐
│                    Base Bdev (Nvme0n1)                       │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  GPT Header (LBA 1)                                  │   │
│  │  - 分區表元數據                                      │   │
│  │  - 分區條目                                          │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  Partition 1 │  │  Partition 2 │  │  Partition 3 │     │
│  │  (Nvme0n1p1) │  │  (Nvme0n1p2) │  │  (Nvme0n1p3) │     │
│  │              │  │              │  │              │     │
│  │  GUID:       │  │  GUID:       │  │  GUID:       │     │
│  │  6527994e... │  │  6527994e... │  │  (其他類型)  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  GPT Backup Header (最後一個 LBA)                     │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 工作流程

```
1. SPDK 應用程式啟動
   │
   ▼
2. GPT 模組自動掃描所有已附加的 bdev
   │
   ▼
3. 檢測到 GPT 分區表？
   ├─ 是 → 為每個 SPDK 類型的分區創建虛擬 bdev
   └─ 否 → 跳過
   │
   ▼
4. 虛擬 bdev 自動可用 (例如: Nvme0n1p1, Nvme0n1p2)
```

### SPDK GPT 分區表 {#bdev_ug_gpt}

SPDK 分區類型 GUID 是 `6527994e-2c5a-4eec-9613-8f5944074e8b`。現有的 SPDK bdev
可以通過 NBD 作為 Linux 塊設備公開，然後可以使用
標準分區工具進行分區。分區後，需要刪除 bdev 並
再次附加，以便 GPT bdev 模組看到任何更改。必須首先加載 NBD 內核模組。
要創建 NBD bdev，用戶應使用 `nbd_start_disk` RPC 命令。

範例命令

`rpc.py nbd_start_disk Malloc0 /dev/nbd0`

這將在 `/dev/nbd0` 塊設備下公開 SPDK bdev `Malloc0`。

要刪除 NBD 設備，用戶應使用 `nbd_stop_disk` RPC 命令。

範例命令

`rpc.py nbd_stop_disk /dev/nbd0`

要顯示完整或指定的 nbd 設備列表，用戶應使用 `nbd_get_disks` RPC 命令。

範例命令

`rpc.py nbd_stop_disk -n /dev/nbd0`

### 使用 NBD 創建 GPT 分區表 {#bdev_ug_gpt_create_part}

~~~bash
# 通過 JSON-RPC 將 bdev Nvme0n1 公開為內核塊設備 /dev/nbd0
rpc.py nbd_start_disk Nvme0n1 /dev/nbd0

# 創建 GPT 分區表。
parted -s /dev/nbd0 mklabel gpt

# 添加一個消耗 50% 可用空間的分區。
parted -s /dev/nbd0 mkpart MyPartition '0%' '50%'

# 將分區類型更改為 SPDK GUID。
# sgdisk 是 gdisk 包的一部分。
sgdisk -t 1:6527994e-2c5a-4eec-9613-8f5944074e8b /dev/nbd0

# 停止 NBD 設備（停止導出 /dev/nbd0）。
rpc.py nbd_stop_disk /dev/nbd0

# 現在 Nvme0n1 配置了 GPT 分區表，並且
# 第一個分區將自動公開為
# SPDK 應用程式中的 Nvme0n1p1。
~~~

## iSCSI bdev {#bdev_config_iscsi}

### 概述

SPDK iSCSI bdev 驅動程式依賴於 libiscsi，因此預設不啟用。
為了使用它，使用額外的 `--with-iscsi-initiator` 配置選項構建 SPDK。

iSCSI bdev 允許 SPDK 應用程式作為 iSCSI 啟動器連接到遠程 iSCSI 目標，
將遠程存儲設備作為本地塊設備使用。

### iSCSI 架構示意圖

```
┌─────────────────────────────────────────────────────────────┐
│                    SPDK Application                         │
│                                                             │
│  ┌──────────────────────────────────────┐                  │
│  │        iSCSI Bdev (iSCSI0)           │                  │
│  │        (本地塊設備介面)               │                  │
│  └──────────────┬───────────────────────┘                  │
└─────────────────┼───────────────────────────────────────────┘
                  │
                  │ iSCSI Protocol (TCP/IP)
                  │
┌─────────────────▼───────────────────────────────────────────┐
│              iSCSI Target (遠程存儲)                          │
│                                                             │
│  ┌──────────────────────────────────────┐                  │
│  │  Target IQN:                          │                  │
│  │  iqn.2016-06.io.spdk:disk1            │                  │
│  │                                       │                  │
│  │  LUN 0 ────────────────┐             │                  │
│  │  LUN 1 ────────────┐   │             │                  │
│  │  LUN 2 ────────┐   │   │             │                  │
│  └───────────────┼───┼───┼──────────────┘                  │
│                  │   │   │                                 │
│         ┌────────┘   │   └────────┐                        │
│         │            │            │                        │
│    ┌────▼────┐  ┌────▼────┐  ┌───▼────┐                  │
│    │ Storage │  │ Storage │  │ Storage│                  │
│    │ Device 1│  │ Device 2│  │ Device3│                  │
│    └─────────┘  └─────────┘  └────────┘                  │
└─────────────────────────────────────────────────────────────┘
```

### URL 格式說明

以下命令在給定的 iSCSI URL 處從單個 LUN 創建 `iSCSI0` bdev
，報告的啟動器 IQN 為 `iqn.2016-06.io.spdk:init`。

`rpc.py bdev_iscsi_create -b iSCSI0 -i iqn.2016-06.io.spdk:init --url iscsi://127.0.0.1/iqn.2016-06.io.spdk:disk1/0`

URL 採用以下格式：
`iscsi://[<username>[%<password>]@]<host>[:<port>]/<target-iqn>/<lun>`

**URL 組成部分說明**：
- `iscsi://`: 協議前綴
- `[<username>[%<password>]@]`: 可選的認證資訊（CHAP 認證）
- `<host>`: iSCSI 目標主機的 IP 地址或主機名
- `[:<port>]`: 可選的端口號（預設為 3260）
- `<target-iqn>`: 目標的 IQN（iSCSI Qualified Name）
- `<lun>`: 邏輯單元號（Logical Unit Number）

**範例 URL**：
- `iscsi://192.168.1.100/iqn.2016-06.io.spdk:disk1/0` - 基本連接
- `iscsi://user%pass@192.168.1.100:3260/iqn.2016-06.io.spdk:disk1/0` - 帶認證的連接

### 使用場景

- **遠程存儲訪問**: 通過網路訪問遠程存儲設備
- **存儲虛擬化**: 將遠程存儲作為本地塊設備使用
- **災難恢復**: 訪問備份站點的存儲資源
- **測試環境**: 在沒有本地存儲的環境中測試應用程式

## Linux AIO bdev {#bdev_config_aio}

### 概述

SPDK AIO bdev 驅動程式通過 Linux AIO 提供對 Linux 內核塊
設備或 Linux 文件系統上的文件的 SPDK 塊層訪問。請注意，使用 O_DIRECT，因此繞過了 Linux 頁面快取。
這種模式可能與
不使用用戶空間驅動程式的用戶空間目標一樣接近典型的基於內核的目標。要創建 AIO bdev RPC 命令，應使用 `bdev_aio_create`。

### AIO 架構示意圖

```
┌─────────────────────────────────────────────────────────────┐
│                    SPDK Application                         │
│                                                             │
│  ┌──────────────────────────────────────┐                  │
│  │        AIO Bdev (aio0)                │                  │
│  │        (SPDK 塊設備介面)               │                  │
│  └──────────────┬───────────────────────┘                  │
└─────────────────┼───────────────────────────────────────────┘
                  │
                  │ Linux AIO (libaio)
                  │ O_DIRECT (繞過頁面快取)
                  │
┌─────────────────▼───────────────────────────────────────────┐
│              Linux Kernel                                    │
│                                                             │
│  ┌──────────────────────────────────────┐                  │
│  │  選項 1: 塊設備                        │                  │
│  │  /dev/sda, /dev/nvme0n1 等            │                  │
│  └──────────────────────────────────────┘                  │
│                                                             │
│  ┌──────────────────────────────────────┐                  │
│  │  選項 2: 文件系統文件                  │                  │
│  │  /tmp/file, /mnt/storage/data.img     │                  │
│  │  (需要指定塊大小)                      │                  │
│  └──────────────────────────────────────┘                  │
│                                                             │
│  ┌──────────────────────────────────────┐                  │
│  │  底層存儲設備                          │                  │
│  │  (SSD, HDD, 等)                       │                  │
│  └──────────────────────────────────────┘                  │
└─────────────────────────────────────────────────────────────┘
```

### 使用說明

**從塊設備創建**：

範例命令

`rpc.py bdev_aio_create /dev/sda aio0`

此命令將從 /dev/sda 創建 `aio0` 設備。

**從文件創建**：

`rpc.py bdev_aio_create /tmp/file file 4096`

此命令將從 /tmp/file 創建塊大小為 4096 的 `file` 設備。

**注意事項**：
- 使用 O_DIRECT 標誌，數據直接從設備讀寫，繞過 Linux 頁面快取
- 文件必須對齊到塊大小邊界（通常為 512 字節或 4KB）
- 文件大小應該是塊大小的整數倍
- 性能可能不如原生 SPDK 驅動程式（如 NVMe），但提供了與傳統內核存儲的兼容性

### 使用場景

- **兼容性測試**: 測試 SPDK 應用程式與傳統 Linux 存儲的兼容性
- **文件作為存儲**: 將文件系統中的文件作為塊設備使用
- **開發和調試**: 在開發環境中快速創建測試用的塊設備
- **遷移路徑**: 從傳統內核存儲遷移到 SPDK 的過渡方案

要刪除 aio bdev，請使用 bdev_aio_delete 命令。

`rpc.py bdev_aio_delete aio0`

## OCF 虛擬 bdev {#bdev_config_cas}

### 概述

OCF 虛擬 bdev 模組基於 [Open CAS Framework](https://github.com/Open-CAS/ocf) - 一個
高性能塊存儲快取元函式庫。
要啟用該模組，請使用 `--with-ocf` 標誌配置 SPDK。
OCF bdev 可用於為任何底層 bdev 啟用快取。

### OCF 快取架構

```
┌─────────────────────────────────────────────────────────────┐
│                    Application                               │
│              (讀取/寫入請求)                                 │
└──────────────────────┬──────────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────────┐
│              OCF Bdev (Cache1)                               │
│                                                              │
│  ┌──────────────────────────────────────┐                   │
│  │  快取邏輯                             │                   │
│  │  - 快取行大小: 4KiB (預設)            │                   │
│  │  - 快取模式: Write-Through            │                   │
│  │  - 元數據: 非易失性 (預設禁用)         │                   │
│  └──────────────────────────────────────┘                   │
│                       │                                     │
│         ┌─────────────┼─────────────┐                       │
│         │             │             │                       │
│         ▼             │             ▼                       │
│  ┌─────────────┐     │     ┌─────────────┐                │
│  │ 快取設備     │     │     │ 核心設備     │                │
│  │ (Malloc0)    │     │     │ (Nvme0n1)   │                │
│  │              │     │     │             │                │
│  │ 快速存儲     │     │     │ 持久存儲    │                │
│  │ (RAM/SSD)    │     │     │ (NVMe SSD)  │                │
│  └─────────────┘     │     └─────────────┘                │
│                      │                                     │
│         └────────────┘                                     │
│              (Write-Through: 同時寫入兩者)                  │
└─────────────────────────────────────────────────────────────┘
```

### 快取模式說明

**Write-Through (寫透模式)**：
- 數據同時寫入快取設備和核心設備
- 讀取時先檢查快取，命中則直接返回，未命中則從核心設備讀取並更新快取
- 優點：數據一致性高，不會丟失數據
- 缺點：寫入性能提升有限

**Write-Back (寫回模式)**：
- 數據先寫入快取設備，稍後再寫入核心設備
- 讀取時優先從快取讀取
- 優點：寫入性能大幅提升
- 缺點：需要處理快取數據的持久化，系統故障可能導致數據丟失

以下是創建 OCF bdev 的範例命令：

`rpc.py bdev_ocf_create Cache1 wt Malloc0 Nvme0n1`

此命令將創建新的 OCF bdev `Cache1`，將 bdev `Malloc0` 作為快取設備
，將 `Nvme0n1` 作為核心設備，初始快取模式為 `Write-Through`。
`Malloc0` 將用作 `Nvme0n1` 的快取，因此寫入 `Cache1` 的數據將存在
在 `Nvme0n1` 上。
預設情況下，OCF 將配置為快取行大小等於 4KiB
，並且將禁用非易失性元數據。

要刪除 `Cache1`：

`rpc.py bdev_ocf_delete Cache1`

在刪除期間，OCF 快取將停止，所有快取的數據將寫入核心設備。

請注意，OCF 對每個設備都有 RAM 要求。更多詳細資訊可以在
[OCF 文檔](https://open-cas.github.io/guide_system_requirements.html) 中找到。

## Malloc bdev {#bdev_config_malloc}

### 概述

Malloc bdev 是 ramdisk（內存磁盤）。由於其性質，它們是易失性的。它們是從提供給 SPDK
應用程式的 hugepage 記憶體創建的。

### Malloc Bdev 架構示意圖

```
┌─────────────────────────────────────────────────────────────┐
│                    SPDK Application                         │
│                                                             │
│  ┌──────────────────────────────────────┐                  │
│  │        Malloc Bdev (Malloc0)         │                  │
│  │        (RAM-based 塊設備)            │                  │
│  └──────────────┬───────────────────────┘                  │
└─────────────────┼───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│              Hugepage Memory                                 │
│                                                             │
│  ┌──────────────────────────────────────┐                  │
│  │  配置: 64 MB, 512 字節塊大小          │                  │
│  │                                       │                  │
│  │  ┌──────┐ ┌──────┐ ┌──────┐         │                  │
│  │  │Block │ │Block │ │Block │  ...    │                  │
│  │  │  0   │ │  1   │ │  2   │         │                  │
│  │  └──────┘ └──────┘ └──────┘         │                  │
│  │                                       │                  │
│  │  特性:                                │                  │
│  │  • 極快的讀寫速度                     │                  │
│  │  • 易失性（重啟後數據丟失）           │                  │
│  │  • 無物理存儲限制                     │                  │
│  └──────────────────────────────────────┘                  │
└─────────────────────────────────────────────────────────────┘
```

### 參數說明

創建 malloc bdev 的範例命令：

`rpc.py bdev_malloc_create -b Malloc0 64 512`

**參數解釋**：
- `-b Malloc0`: bdev 的名稱
- `64`: 大小（MB），此例為 64 MB
- `512`: 塊大小（字節），此例為 512 字節

**注意事項**：
- 數據存儲在內存中，應用程式關閉或系統重啟後數據會丟失
- 使用 hugepage 內存以獲得更好的性能
- 適合用於測試、快取、臨時數據存儲等場景
- 大小受系統可用 hugepage 內存限制

### 使用場景

- **性能測試**: 測試應用程式邏輯，排除存儲 I/O 瓶頸
- **開發調試**: 快速創建測試環境，無需物理存儲設備
- **快取層**: 作為其他存儲設備的快取層（通常與 OCF 等模組配合使用）
- **臨時存儲**: 存儲臨時數據，不需要持久化

刪除 malloc bdev 的範例命令：

`rpc.py bdev_malloc_delete Malloc0`

## Null {#bdev_config_null}

### 概述

SPDK null bdev 驅動程式是一個虛擬塊 I/O 目標，它丟棄所有寫入並返回未定義
的數據進行讀取。它對於以最小的塊
設備開銷對 bdev I/O 堆棧的其餘部分進行基準測試以及測試無法輕鬆使用 Malloc bdev 創建的配置很有用。
要創建 Null bdev RPC 命令，應使用 `bdev_null_create`。

### Null Bdev 架構示意圖

```
┌─────────────────────────────────────────────────────────────┐
│                    SPDK Application                         │
│                                                             │
│  ┌──────────────────────────────────────┐                  │
│  │        Null Bdev (Null0)              │                  │
│  │        (虛擬塊設備)                    │                  │
│  └──────────────┬───────────────────────┘                  │
└─────────────────┼───────────────────────────────────────────┘
                  │
                  │ 寫入: 數據被丟棄
                  │ 讀取: 返回未定義數據
                  │
┌─────────────────▼───────────────────────────────────────────┐
│              /dev/null (概念上)                               │
│                                                             │
│  ┌──────────────────────────────────────┐                  │
│  │  特性:                                │                  │
│  │  • 寫入操作: 立即完成，數據丟棄        │                  │
│  │  • 讀取操作: 立即返回，數據未定義      │                  │
│  │  • 無實際存儲: 不佔用內存或磁盤        │                  │
│  │  • 極低開銷: 最小化 I/O 處理開銷      │                  │
│  │  • 可配置大小: 支持超大容量            │                  │
│  └──────────────────────────────────────┘                  │
└─────────────────────────────────────────────────────────────┘
```

### 使用說明

範例命令

`rpc.py bdev_null_create Null0 8589934592 4096`

此命令將創建一個 8 petabyte 的 `Null0` 設備，塊大小為 4096。

**參數解釋**：
- `Null0`: bdev 的名稱
- `8589934592`: 大小（字節），約 8 PB（8 * 1024^5 字節）
- `4096`: 塊大小（字節）

**注意事項**：
- 寫入操作會立即成功，但數據不會被存儲
- 讀取操作會立即返回，但返回的數據內容未定義
- 不消耗實際存儲空間（內存或磁盤）
- 適合用於測試 I/O 路徑的性能，排除存儲設備的影響

### 使用場景

- **性能基準測試**: 測試 I/O 堆棧的性能，排除存儲設備延遲的影響
- **協議測試**: 測試上層協議（如 NVMe-oF、iSCSI）的處理能力
- **壓力測試**: 測試系統在高 I/O 負載下的表現
- **配置驗證**: 驗證無法用 Malloc 創建的大容量配置
- **開發調試**: 快速創建測試環境，無需實際存儲

要刪除 null bdev，請使用 bdev_null_delete 命令。

`rpc.py bdev_null_delete Null0`

## NVMe bdev {#bdev_config_nvme}

### 概述

在 SPDK 中，有兩種方法可以基於 NVMe 設備創建塊設備。第一種
方法是連接本地 PCIe 驅動，第二種是連接 NVMe-oF 設備。
在這兩種情況下，用戶都應使用 `bdev_nvme_attach_controller` RPC 命令來實現。

### NVMe Bdev 架構示意圖

```
┌─────────────────────────────────────────────────────────────┐
│                    SPDK Application                         │
│                                                             │
│  ┌──────────────────────────────────────┐                  │
│  │        NVMe Bdev (NVMe1/Nvme0)       │                  │
│  │        (SPDK 塊設備介面)               │                  │
│  └──────────────┬───────────────────────┘                  │
└─────────────────┼───────────────────────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
        ▼                   ▼
┌───────────────┐   ┌──────────────────┐
│  本地 PCIe    │   │   NVMe-oF        │
│               │   │                  │
│  ┌─────────┐ │   │  ┌─────────────┐ │
│  │ NVMe SSD │ │   │  │  RDMA/TCP   │ │
│  │ (PCIe)   │ │   │  │  網路連接   │ │
│  └────┬────┘ │   │  └──────┬──────┘ │
└───────┼───────┘   └─────────┼────────┘
        │                     │
        │ PCIe Bus            │ Network
        │                     │
┌───────▼──────────┐  ┌───────▼──────────┐
│  Physical NVMe   │  │  Remote NVMe     │
│  SSD Device      │  │  Target          │
│  (0000:01:00.0)  │  │  (192.168.100.1) │
└──────────────────┘  └──────────────────┘
```

### 連接本地 PCIe NVMe 設備

範例命令

`rpc.py bdev_nvme_attach_controller -b NVMe1 -t PCIe -a 0000:01:00.0`

此命令將創建系統中物理設備的 NVMe bdev。

**參數說明**：
- `-b NVMe1`: bdev 的名稱
- `-t PCIe`: 傳輸類型（PCIe）
- `-a 0000:01:00.0`: PCIe 設備的 BDF（Bus:Device.Function）地址

**BDF 地址格式**：
- `0000`: PCIe 域（通常為 0000）
- `01`: 總線號
- `00`: 設備號
- `0`: 功能號

可以使用 `lspci | grep -i nvme` 命令查看系統中的 NVMe 設備及其 BDF 地址。

### 連接 NVMe-oF 設備

`rpc.py bdev_nvme_attach_controller -b Nvme0 -t RDMA -a 192.168.100.1 -f IPv4 -s 4420 -n nqn.2016-06.io.spdk:cnode1`

此命令將創建 NVMe-oF 資源的 NVMe bdev。

**參數說明**：
- `-b Nvme0`: bdev 的名稱
- `-t RDMA`: 傳輸類型（RDMA 或 TCP）
- `-a 192.168.100.1`: 目標主機的 IP 地址
- `-f IPv4`: 地址族（IPv4 或 IPv6）
- `-s 4420`: 服務端口號（預設為 4420）
- `-n nqn.2016-06.io.spdk:cnode1`: 目標的 NQN（NVMe Qualified Name）

**NVMe-oF 傳輸類型**：
- `RDMA`: 使用 RDMA 協議（InfiniBand、RoCE、iWARP）
- `TCP`: 使用 TCP/IP 協議

### 多路徑支持

SPDK NVMe bdev 驅動程式提供多路徑功能。請參閱
@ref nvme_multipath 了解詳細資訊。

**多路徑優勢**：
- **高可用性**: 單一路徑故障時自動切換到其他路徑
- **負載均衡**: 在多個路徑間分配 I/O 負載
- **性能提升**: 利用多個網路連接提高總體帶寬

### 刪除 NVMe 控制器

要刪除 NVMe 控制器，請使用 bdev_nvme_detach_controller 命令。

`rpc.py bdev_nvme_detach_controller Nvme0`

此命令將刪除名為 Nvme0 的 NVMe bdev。

**注意事項**：
- 刪除控制器前，確保沒有應用程式正在使用該 bdev
- 如果該 bdev 上創建了虛擬 bdev（如加密、快取等），需要先刪除虛擬 bdev

### NVMe bdev 字符設備 {#bdev_config_nvme_cuse}

NVMe bdev 字符設備功能允許將 SPDK NVMe bdev 作為 Linux 字符設備（/dev/spdk*）公開，
使應用程式可以像訪問普通字符設備一樣訪問 SPDK bdev。

**使用場景**：
- 與需要字符設備介面的應用程式集成
- 使用標準 Linux 工具（如 dd、fio）測試 SPDK bdev
- 提供與傳統內核驅動程式的兼容性

範例命令

（此功能需要額外的配置和編譯選項，具體使用方法請參考相關文檔）

## 常見使用場景和最佳實踐 {#bdev_ug_best_practices}

### 堆疊 Bdev 的組合使用

SPDK bdev 支持將多個虛擬 bdev 堆疊組合，創建複雜的存儲配置：

```
應用程式
   │
   ▼
加密 Bdev (CryNvmeA)
   │
   ▼
OCF 快取 Bdev (Cache1)
   │
   ├───► 快取設備 (Malloc0)
   │
   └───► 核心設備 (Nvme0n1)
          │
          ▼
       GPT 分區 (Nvme0n1p1)
```

**範例配置流程**：
1. 創建基礎 NVMe bdev
2. 在 NVMe bdev 上創建 GPT 分區
3. 在分區上創建 OCF 快取
4. 在快取上創建加密層

### 性能優化建議

1. **塊大小對齊**: 確保應用程式的 I/O 大小與 bdev 塊大小對齊，以獲得最佳性能

2. **隊列深度**: 根據存儲設備特性調整 I/O 隊列深度
   - NVMe SSD: 通常支持較深的隊列（64-1024）
   - 網路存儲: 需要考慮網路延遲和帶寬

3. **多路徑配置**: 對於關鍵應用，配置多路徑以提高可用性和性能

4. **快取策略**: 根據工作負載特性選擇合適的快取模式
   - 讀多寫少: Write-Through 或 Write-Back
   - 寫多讀少: 可能需要禁用快取或使用 Write-Through

### 故障排除

1. **檢查 bdev 狀態**: 使用 `bdev_get_bdevs` RPC 命令查看所有 bdev 的狀態

2. **查看日誌**: 檢查 SPDK 應用程式的日誌輸出，查找錯誤訊息

3. **驗證配置**: 確認所有必需的模組都已正確加載和配置

4. **測試連接**: 對於網路存儲（如 iSCSI、NVMe-oF），先測試網路連接

### 安全建議

1. **加密密鑰管理**: 
   - 使用安全的密鑰存儲機制
   - 定期輪換加密密鑰
   - 保護密鑰文件的訪問權限

2. **網路安全**:
   - 使用 CHAP 認證連接 iSCSI 目標
   - 在生產環境中使用加密的網路連接（如 IPsec）

3. **訪問控制**: 限制對 SPDK RPC 介面的訪問，使用適當的認證機制

## 總結 {#bdev_ug_summary}

SPDK 塊設備層提供了一個靈活且強大的框架，用於管理和訪問各種類型的存儲設備。
通過物理 bdev、虛擬 bdev 和堆疊 bdev 的組合，可以創建滿足各種需求的存儲配置。

**關鍵要點**：
- Bdev 提供了統一的塊設備抽象層
- 支持多種物理和虛擬存儲設備
- 可以通過堆疊創建複雜的存儲配置
- 所有配置通過 JSON-RPC 完成
- 提供高性能的無鎖 I/O 處理

**下一步**：
- 探索 @ref bdev_module 了解如何創建自定義 bdev 模組
- 查看 @ref jsonrpc_components_bdev 了解完整的 RPC API
- 參考具體的 bdev 模組文檔了解詳細配置選項
